# Multiclass Negative Results

Purpose: record seams and approaches that were tried for clean-room multiclass client work and did not solve the target problem, so we do not keep re-running the same experiments.

Status meanings:
- `disproven`: runtime or disassembly evidence shows this seam was the wrong layer or did not affect the target surface.
- `inconclusive`: tried, but the evidence was weak or a known precondition made the result non-final.
- `stale`: older result that may no longer apply after later hook changes; do not treat it as current proof.

## Current Ledger

| Surface | Seam / approach tried | Status | Evidence | What we learned | Preferred replacement direction |
| --- | --- | --- | --- | --- | --- |
| `/who` row class text | Internal lookup callsite patch set inside the misidentified `0x536310` `LeftClickedOnPlayerSurrogate`: `0x5364e7`, `0x5365c2`, `0x536601` | `disproven` | Live runs around `2026-05-21 17:27` and `17:37` installed the callsite hooks, but produced no `UiClassDisplayTrace` for `WhoClassName`; `/who` still decoded as native `class_id=3` | Patching internal lookup sites inside the broader builder was not enough to rewrite visible `/who` class text, and the parent seam itself was mislabeled | Track real `WhoClassName` subject context at wrapper entry and/or move closer to the actual `EQ_WhoClassName` producer seam THJ detoured |
| `/who` row class text | Narrowed single-callsite patch on `WhoClassNameClassLookupCallsiteA` at `0x5364e7` | `disproven` | Live runs around `2026-05-21 17:47` and `17:53` still showed no `UiClassDisplayTrace` for `WhoClassName`; `/who` still decoded as native `class_id=3` | Even the class-id-fed branch alone was still the wrong rewrite layer when patched as a raw callsite | Keep the newer wrapper-context plus filtered-lookup model; if that still fails, pivot again toward the real `EQ_WhoClassName` function-pointer seam |
| `/who` row class text | Applying the local player's multiclass full-name string directly at the hot shared lookup caller `0x477e6` whenever the `OP_WhoAllResponse` correlation window was active | `disproven` | Live log on `2026-05-28` showed `WhoAllClassDisplayTrace ... caller_rva=0x477e6 ... override_applied=true formatted="Paladin/Shadow Knight/Monk" reason="who_all_class_label_local_player_display_applied"` even for other players whose packet entries logged native classes `7` and `3` | The shared lookup seam is real, but that caller is subject-free. Reusing the local player's class mask there paints every `/who` row with the wrong multiclass string | Keep the same `0x477e6 -> 0x7d0660` seam, but consume stored `OP_WhoAllResponse` entries in order and format by entry name plus native class |
| `/who` row class text | Treating the visible shared lookup as a single hot caller return `0x477e6` | `disproven` | Clean-room disassembly of `eqgame.exe` at `VA 0x447770-0x447a26` on `2026-05-28` proved the `/who` class text uses a jump-table family of adjacent `call 0x7d0660` sites, and live log at `2026-05-28 14:18:25` showed Warrior rows returning through `caller_rva=0x47789` while our correlation window drained on unrelated lookups before the later `0x477e6` path | The seam is a family of class-string lookups, not one return address. Matching only `0x477e6` misses other native classes and lets the queued `/who` entries fall out of sync | Recognize the proved class-string caller family by RVA block plus class string-id range, and only consume `/who` correlation on those callers |
| Spellbar/spellbook unlock for native-melee plus caster mask | Auth-time `CEverQuest::InterpretCmd` reuse of slash aliases `/gems` and `/book` | `disproven` | Live run on `2026-05-26` logged `MulticlassCasterUiAccess ... opened_spell_gems=true opened_spellbook=true`, but the user confirmed `/gems` opens the `GemsGameWnd` mini-game and `/book` does nothing for the intended spellbook surface | These slash aliases are not the real spellbar/spellbook toggle path in this ROF2 client, so command success is not proof that the caster UI became available | Drive the actual `CastSpellWnd` / `SpellBookWnd` windows directly, or prove the stock spell-window toggle command IDs before using `EQExecuteCmd` |
| Spellbar/spellbook unlock for native-melee plus caster mask | Single deferred retry from the hot local `GetClassDesc` / class-display path after auth | `disproven` | Live repro on `2026-05-27` logged both the auth-time attempt and the later `MulticlassCasterUiAccessDeferredRetry trigger="GetClassDesc"` attempt with the same `reason="no_ui_attempt_available"`, `cast_spell_window_available=false`, `spellbook_window_available=false`, and command-table `non_empty_count=0` | Reaching a later local-player/local-name-ready class-display seam once is still not enough to prove the spell UI globals or the command registry are initialized | Keep the same safe retry trigger sites only as a bounded repeated window, or move to a later true main-UI-ready lifecycle seam if repeated retries still never see live spell UI state |
| Spellbar/spellbook unlock for native-melee plus caster mask | Bounded repeated retry window driven only by local class-display helper surfaces such as `GetClassDesc` | `disproven` | Live repro around `2026-05-27 09:44` stayed logged in after `MulticlassCasterUiAccessDeferredRetry trigger="GetClassDesc" attempt_number=1 success=false`, but no later retry lines or spell-window gate traces appeared; the log simply went cold for this feature while other in-world hooks kept firing | The retry budget and cooldown existed, but they were not self-advancing. Staying logged in was not enough because the retry loop only ran when a hooked class-display helper executed again | Move the pending retry pulse onto an already-proved hot in-world seam, such as packet receive dispatch, instead of waiting for another optional class-display callback |
| Inventory title / local full-name class text | Direct UI-text interception: guessed inventory refresh path, `CXWnd::SetWindowTextA`, and `CXStr::Assign` at `0x405d90` | `disproven` | These hooks were retired after repeated failures to change the visible inventory class title; THJ evidence also points to producer detours, not final-control text writes | The formatting was not the issue; this was the wrong layer | Resume only when a real inventory-adjacent producer seam is pinned, likely the clean-room equivalent of THJ `EQ_CharSelectClassNameFunc` |
| Remaining inventory-adjacent class text | Treating `InventoryWindow + 0x2cc/+0x2d0` as live class-title members | `disproven` | `/home/zutfen/inventory_title_path_report.md` proved `0x6909c0` binds those offsets to alt-currency controls, while `IDW_ClassTitle1/2` belong to `ItemDisplayWindow` at `0x6a0d80` | The old `CInventoryWnd::ClassTitle1/2` premise does not match this ROF2 client | Move the target surface to `ItemDisplayWindow` and the proven producer chain ending at the THJ-confirmed seam `base + 0x2a9540` (`EQ VA 0x6a9540`) |
| Remaining inventory-adjacent class text | Assuming the visible item-display class path uses `0x514dc0` (`GetClassDesc`) or `0x5153c0` (`GetClassThreeLetterCode`) | `disproven` | `/home/zutfen/inventory_title_path_report.md` proved `0x6ae100 -> 0x6a9540` builds class text from a bitmask + string-id table via `0x7d0660`, with no proven calls to either helper | Further helper-caller hunting was drift for this surface | Keep discovery/hooking focused on `ItemDisplayWindow` refresh and row-builder seams instead |
| Inventory summary pane class text | Treating `ItemDisplayWindow` / item inspection as the active blocker for the user-visible `Driton / 1 / Paladin / Tunare` problem | `disproven` | The user confirmed the real target is the top-left summary pane in the main inventory window, not item inspection; later runs also showed item-display hooks could install without affecting that surface | `ItemDisplayWindow` may still be a real multiclass surface, but it is the wrong active surface for this bug | Focus on `CInventoryWindow` summary refresh / producer paths instead |
| Inventory / char-select-adjacent class text | Treating the current `char_select_class_name_func` discovery slot as proven THJ `EQ_CharSelectClassNameFunc` | `inconclusive` | The currently recovered seam behaves like a progression-selection writer and is useful there, but it is not proven to be THJ's real inventory/title producer seam | Do not generalize the progression-selection seam into proof for other UI surfaces | Keep it scoped to progression selection until a distinct inventory/title producer is recovered |
| Character select multiclass class list | Progression-selection callsite hook at `0x3212b6` feeding `ProgressionSelectionClassLookupCallsiteHook` | `disproven` | Live repro on `2026-05-22 09:18` showed `hook_manager: ProgressionSelectionClassLookupCallsite callsite hook installed`, but the log contained no `ProgressionSelectionTrace` lines at all while character select still rendered plain `Paladin`; meanwhile only the early pre-auth `GetClassDesc` caller `0x18e554` and later in-world three-letter caller `0x2781a6` fired | This client flow is not using the current progression-selection seam for the visible character-select class list | Pivot to the actual post-auth character-select refresh/repaint path or to later full-name producers that run after `OP_ServerAuthStats` lands |
| Character select multiclass class list | Validated `CharSelectClassNameFunc` entry trace at `0x321210` | `disproven` | Live repro on `2026-05-22 09:33` logged `hook_manager: char select class-name function trace installed target=CharSelectClassNameFunc address=0x721210 target_rva=0x321210` and `entry_trace=true callsite_patch=true`, but the log contained zero `CharSelectClassNameFuncTrace` lines while character select still rendered plain `Paladin` | The broader validated `0x321210` producer family is also cold in this visible class-list flow, not just the inner `0x3212b6` branch | Pivot away from the progression / `CharSelectClassNameFunc` family and bias toward other post-auth character-select refresh or later full-name producer seams |
| Character select multiclass class list | Late full-name `GetClassDesc` callsite patches at `0x3249ac`, `0x324d84`, and `0x3252ab` | `disproven` | Live repro on `2026-05-22 09:55` logged `late_full_name_a=true late_full_name_b=true late_full_name_c=true`, but the log contained zero `CharSelectLateFullNameTrace` lines while character select still fell back to the early pre-auth `0x18e554` full-name helper and later `0x2781a6` three-letter self surface | Those later helper callsites are cold too; the visible character-select full-name list is not using this whole late-helper family in the active client flow | Pivot away from helper-level char-select probes and toward parent refresh/build seams such as `CCharacterListWnd` list-update / repaint paths |
| Local full-name class display | `0x514dc0` (`GetClassDesc`) caller `0x18e554` early full-name path, before `OP_ServerAuthStats` is available | `inconclusive` | Runs around `2026-05-21 17:37`, `17:47`, and `17:53` showed `caller_rva=0x18e554` firing before assigned multiclass data arrived, so override could not apply | Early fallback here does not prove the formatter or semantic override is wrong; timing was bad | Re-test only after authoritative class-mask data is present, or find a later producer seam for the same surface |
| Guild manager roster class column | Treating `GetClassDesc` caller return `0x2843ff` as a known local-self three-letter surface | `disproven` | Live log on `2026-05-28` showed `UiClassHelperCallerCatalogTrace helper=GetClassDesc caller_rva=0x2843ff ... override_applied=true formatted="PAL/SHD/MNK"` for multiple different guild members, while clean-room disassembly at `VA 0x6843f0` proved the caller is a per-member row builder with the member name at `EBX+0x12` and native class at `EBX+0x5c` | This path is not a self-summary surface. It is the guild roster's per-row class producer, so local-player substitution is structurally wrong there | Patch the specific callsite at `0x2843fa` and format from the row context's member name plus native class instead of using a shared local-player helper override |
| Guild manager roster class column | Letting the generic `GetClassDesc` local-primary fallback (`reason="requested_class_matches_local_primary"`) run at caller `0x2843ff` | `disproven` | Live log on `2026-05-28 14:11:18` showed the hot guild roster caller repeatedly rendering remote Paladins as `Paladin/Shadow Knight/Monk` solely because their requested native class matched the local player's primary class id `3` | Even without a special guild hook installed, the broad local-primary fallback is still too permissive for this remote roster surface | Bypass the generic local-primary override for caller `0x2843ff` and leave the roster on native classes until a real per-member remote identity source is proven |
| Guild manager roster class column | Reusing the bypass-only `GetClassDesc` branch that serves `char_select_cached_full_name_display` for caller `0x2843ff` | `disproven` | Live log on `2026-05-28 14:18:22` showed the hot guild roster caller rendering rows like `Paladin/Shadow Knight/Monk` with `local_class_copied=false` and `reason="char_select_cached_full_name_display"` even after the local-primary fallback was bypassed | The guild roster and early char-select both happened to opt into the same bypass flag, but they are not semantically interchangeable surfaces. Char-select's native-class cache helper cannot be allowed to spill into per-member guild rows | Gate `TryBuildCachedCharSelectFullNameDisplayAscii` to the proved char-select caller `0x18e554` only, and keep guild roster on native class until a real per-member remote source is proven |
| Guildmaster trainer access | Reusing `EQ_Character::IsClassUsablePredicate` / generic item-style class gates for the trainer rejection | `disproven` | ROF2 disassembly around string id `1467` ("not a member of the %1 class guild") and the later live class-guild rejection path at `0x53763f` proved the failure does not flow through the already-hooked generic class predicate | Broadening the generic class-usability hook would be the wrong layer and would risk unrelated item/spell/UI contexts | Keep trainer work scoped to the dedicated guildmaster click flow |
| Guildmaster trainer access | Whole-validator detour at `0x5b0200` / RVA `0x001b0200` plus its inner branches `0x5b0298` and `0x5b0304` | `disproven` | Live repros with the validator hook installed still produced the `0x53763f -> 0x514dc0` rejection-text path, but no `MulticlassTrainerHook` and no `OP_GMTraining` send lines. Wider disassembly then proved the real hard gate earlier in the click flow at `0x5374b9`, where the client compares the clicked guildmaster class against an active-guildmaster class lookup and jumps straight to the rejection text on mismatch | The validator seam is too late or simply cold for the failing Monk/Magician path, so forcing it cannot guarantee the client ever reaches the GM-training send path | Patch the earlier active-guildmaster class-match callsite at `0x5374a1` / RVA `0x001374a1` instead, and keep the override local to that single trainer-class lookup |
| Guildmaster trainer skill list | Treating the remaining "Paladin rows only" bug as a server `OP_GMTraining` payload problem after the access gate was solved | `disproven` | Live packet decode on `2026-05-22 15:11` showed the Monk GM `OP_GMTraining` reply already contained Monk-only skill caps such as `Block`, `DragonPunch`, `FeignDeath`, `Mend`, `RoundKick`, and `TigerClaw`, while the visible train window still rendered Paladin rows | Once access succeeds, the stock train window is still consulting a local class source after a good server packet arrives, so further server packet changes would be drift | Keep the packet and trainer gate as-is and patch only the active training-session local class source the stock train window uses |
| Offhand secondary weapon equip | Treating `InvSlotHandleLButtonCoreLateBranchGateB` at callsite `0x29604f` as the first hot reject seam for slot-14 offhand weapons | `disproven` | Live repro on `2026-05-22 20:07` showed the `PAL/MNK/MAG` offhand sword click reached slot `14`, passed `LateBranchGateA`, then failed earlier at `InvSlotHandleLButtonCoreLateBranchPrep` with `original_result=0`; no slot-14 `MoveItemSend` and no `LateBranchGateB` observation occurred on that path | The earlier prep helper is the real pre-send blocker, so a GateB-only override is one step too late for the failing offhand weapon path | Move the narrow secondary-weapon override and trace focus up to `InvSlotHandleLButtonCoreLateBranchPrep` and keep GateB only as a later fallback seam |
| Offhand secondary weapon equip | Post-send helper family at `0x299851`, `0x299881`, and `0x299904` targeting `0x3e9ae0` (`MoveItemPostSendResolve*`) | `disproven` | Live repro on `2026-05-22 21:41` armed `MoveItemPostSendLocalFocus` for the accepted `33 -> 14` move, but produced zero `MoveItemPostSendResolve` lines, while the earlier local slot-resolve traces still fired immediately before `MoveItemSend` | That helper family does not give a real post-send local correlation window for this move; our focus arm at send time is already too late for the useful part of that path | Pivot to truly later callsites after the `0x2997cf` send return, specifically the `0x299992` / `0x2999ba` local update calls |
| Offhand secondary weapon equip | Whole-object copy helper at `0x597cf0` / RVA `0x00197cf0` (`MoveItemPostSendField144DwordCopy`) | `disproven` | Live run on `2026-05-23 11:14` installed the trace cleanly, but the accepted `33 -> 14` path produced zero `MoveItemPostSendField144DwordCopy` lines while the same activation still proved `branch_after_gate=field_144` and `seed_matches_field_144_17c=true` | The accepted offhand branch is not normalizing the selected `field_144` object through this whole-object dword-copy helper in the window that matters | Pivot to the `0x9caf74` object-family setter/clone paths that directly write `+0x144`, such as `0x555f30`, instead of broader copy-family helpers |
| Authoritative multiclass mask capture | Treating only exact opcode `0x1338` as usable `OP_ServerAuthStats` evidence | `disproven` | Live repro on `2026-05-23 17:34` logged `PacketObserverServerAuthStatsCandidate ... opcode=0 ... statClassesBitmask=0x150`, followed by `PacketObserverServerAuthStatsMissing` and the Ragebringer primary-slot dispatch path showing `assigned_mask=0x0 has_assigned_mask=false` | The payload shape can remain authoritative even when the local opcode reference no longer sees `0x1338`, so exact-opcode-only capture makes downstream class gates fail closed incorrectly | Keep the exact `0x1338` path, but also accept a strict opcode-zero fallback only when the payload is an exact recognized server-auth stat shape containing `statClassesBitmask` |
| Primary equipment class-restricted weapon equip | Treating the older `EquipClick*` / local reject callsites as active proof for the Ragebringer primary-slot failure | `stale` | The SHD/MNK/ROG Ragebringer repro on `2026-05-23 16:16:24` produced no `EquipClick*`, `EquipLocal*`, or local-reject trace lines, while the hot `CInvSlot::HandleLButtonUp` path reached `LateBranchPrep` for slot `13` and did not send `OP_MoveItem` | The older equip-click trace family is not the live evidence path for this drag/drop primary-slot rejection in the current install shape | Keep the behavior fix and proof on the `InvSlotHandleLButtonCore` late branch path; use `OP_MoveItem` send to slot `13` as the acceptance signal |
| Multiclass activated-skill use | Treating the `CharacterZoneClient::HasSkill` action/label callers as the final send path once visibility was opened | `disproven` | Live Kick repro on `2026-05-23` logged `activated skill visibility/use gate override skill_id=30` with the server mask allowing Kick, then the packet correlation window showed unrelated sends and no activated-skill opcode. Disassembly showed the hot action refresh caller only preserves action-slot skill ids; the click path later enters `CharacterZoneClient::UseSkill`, which performs a separate `GetAdjustedSkill` gate before any send. | Visibility and action-slot retention are necessary but not sufficient; the actual pre-send blocker is the adjusted-skill gate inside `UseSkill` for masked secondary/tertiary skills. | Keep the `HasSkill` visibility hook, but patch only the `UseSkill -> GetAdjustedSkill` gate when the authoritative activated-skill mask contains the exact advertised skill id. |
| Player Skills window numeric values | Reusing `CharacterZoneClient::GetAdjustedSkill` / `EvaluateBestAuthoritativeAdjustedSkill` for the visible `Value` column | `disproven` | After widening the local-context predicate to include the local character object, the user still saw `0` in the Skills window. Disassembly of the row-value helper at `0x757710` proved the visible `Value` field is populated from the local-char-data call `0x7577b5 -> 0x584be0`, not from `GetAdjustedSkill`. | The Skills window uses a different numeric producer than the activated-skill send gate; fixing `UseSkill` and visible skill rows requires two separate hooks. | Patch the local-char-data skill-value producer at `0x584be0` and gate the override to the Skills-window row-builder return at `0x3577ba`. |
| Player Skills window numeric values | Inline entry detours on the proved row seams `0x584be0` and `0x757710` | `disproven` | Live startup at `2026-05-23 21:52:02` logged both hooks installed, but the failing repro still produced no `skills window value trace`, no `skills window value override`, and no `skills window row helper trace` lines while the user still saw `0`. | The pinned addresses were real, but intercepting those larger shared entries was not a reliable proof or rewrite layer for this UI path in the active client. | Prefer direct callsite patches on the proved hot callers `0x7577b5`, `0x757c55`, and `0x757e6a`, with entry logging on those patched sites first. |
| Local multiclass mana display | Inline entry detour on `CharacterZoneClient::Cur_Mana` at `0x4442e0` | `disproven` | Live startup on `2026-05-27 11:15:53` logged `CharacterZoneClient::Cur_Mana multiclass mana prologue unsupported; hook disabled`, and the install summary reported `cur_mana_installed=false` while `Max_Mana` and `GetManaRegen` were hot. | The entry seam is real, but this client build’s prologue shape is not compatible with the current inline detour helper, so treating it as the primary current-mana fix path just stalls display work. | Use proved hot `Cur_Mana` callsites such as the player-window EQType and local refresh callers instead of the unsupported entry detour. |
| Local multiclass mana bar visibility | Direct inline detour on the player-window refresh worker at `VA 0x41b670`, inner body `0x41b685` | `disproven` | Live repros on `2026-05-27 12:26` and `12:32` installed the seam cleanly as `player_wnd_refresh_installed=true`, but the client then crashed on zone-in before any `PlayerManaVisibilityForce` or later in-world mana UI traces could fire. Hardening the post-call logic and adding reentrancy guards did not change the failure shape. | The refresh worker does contain the right visibility logic, but entry-detouring that whole lifecycle seam is too fragile for this client during zone-in. | Patch the narrower `0x866610` visibility callsites inside the worker instead, especially the mana gauge and label callsites at `0x41b765`, `0x41b774`, `0x41b8ff`, and `0x41b90d`. |
| Local multiclass mana selection state | Authoritative replay on the early player-window selection-refresh family `0x41c003 -> 0x41bd20` and `0x41c44c -> 0x41bd20` while native selection was still invalid | `disproven` | Live zone-in repro on `2026-05-27 16:35` showed `player_mana_selection_refresh_callsites_installed=2`, then `PlayerManaSelectionRefreshTrace ... replay_needed=true replayed=true ... current_index_after_native=-1 ... current_index_after_replay=-1`, followed by a crash before full zone entry. | This earlier refresh family is a real live seam and useful for tracing, but replaying it under a temporary caster-class override during pre-zone invalid-selection state is not safe and still does not populate `+0x254` / `+0x220`. | Keep the seam as bounded trace-only when native selection is invalid, and move deeper toward the stock writer/reset logic that initializes `+0x254` rather than replaying the whole refresh path. |
| Local multiclass mana selection state | Direct replay of the stock selection writer `0x41bc40` from the hot `Cur_Mana` path after seeding player-window mana state | `disproven` | Live zone-in repro on `2026-05-27 16:54` reached `PlayerManaUiStateSeed count=1` and then crashed immediately, with zero `PlayerManaSelectionRepair` lines ever emitted. | Calling the selection writer directly from the hot mana tick is too early or unsafe during zone-in; it crashes before post-call tracing and is not a viable repair point even though the direct mana state object is valid. | Keep the invalid-selection branch in deferred/trace-only mode and continue tracing the stock reset/write path that naturally populates `+0x254` instead of forcing `0x41bc40` from `Cur_Mana`. |
| Local multiclass mana bar display | Leaving the legacy player-window repair stack armed after the custom pull-style mana UI pivot: refresh-helper callsite `0x41b6fd`, player-window ctor `0x319690`, producers `0x41b920` / `0x41ba60`, selection refresh `0x41bd20`, and selection writer callsite `0x41c8d8` | `disproven` | The live warrior-primary crash repro on `2026-05-28 12:06` still installed all of those seams, and the log died immediately after `PlayerManaUiStateSeed count=1 ...` before any custom EQType mana traces could carry the UI. | Once the mana UI moved to pull authoritative values through the EQType resolver and custom label resolver, continuing to arm the old native player-window repair family only preserved the zone-in crash risk without helping the visible mana bar. | Keep only the safe custom-UI path: authoritative mana producers, player-window visibility callsites, stock gauge EQType override, and custom mana label EQType. |
| Local multiclass mana bar display | Replacing the player-window mana gauge itself with a fully custom gauge `EQType 6667` driven by a new direct local-percent helper | `disproven` | The first live custom-mana repro rendered fallback text (`Unknown Label 6668`) and then the warrior-primary zone-in repro crashed immediately after the custom player-window mana pivot. The safer follow-up reverted the gauge back to stock `EQType 2` while keeping only a custom label path. | A brand-new custom mana gauge path added risk at zone-in and did not buy us anything the stock gauge shell could not already provide. | Keep the stock player-window gauge control and override its value through the existing mana EQType seam; reserve custom EQTypes for label-only data where needed. |
| Local multiclass current-mana inference | Unguarded out-of-band `CharacterZoneClient::Cur_Mana` candidate probing under temporary class overrides inside the player-window EQType mana helper | `disproven` | Live repro on `2026-05-31 21:13:09` showed `CurManaStorageTrace count=2 trigger="authoritative_candidate" override_class_id=4 ... before_combined=3527 after_combined=2866`, proving the class-override probe itself wrote `2866` into the local current-mana slot before the gauge read fell to `81%` | `Cur_Mana` is not a pure getter in this client; probing it for current-mana candidates mutates stored current mana and can poison later UI reads | Preserve and restore local current-mana storage around any out-of-band `Cur_Mana` candidate call, or replace current-mana inference with a non-mutating source instead of class-override probing |
| Local multiclass mana heroic correction | Treating local char-data `HeroicINTBonus` / `HeroicWISBonus` members at `+0x2e98/+0x2e9c` as the raw heroic INT/WIS stat values for the server-style mana delta | `disproven` | Live `WAR/SHM/WIZ` repro on `2026-05-31` moved client mana from `2624` up to `2704` while the server-authoritative value stayed `2664`, proving the added heroic-family delta overshot by exactly one `+40` chunk after reading those fields directly | These members are usable as an eligibility seam, but not as raw heroic stat magnitudes for the mana formula; in this client they represent a doubled bonus-style value relative to the server-facing heroic stat used by mana | Keep the assigned-class heroic-family eligibility fix, but normalize `Heroic*Bonus` before applying mana deltas and avoid treating the raw member contents as server-equivalent heroic stat amounts |
| Item inspect / destroy stability | Forcing `EQ_Character::CanEquip` to return usable on the three inventory-scan callers `0x2085dd`, `0x2086f8`, and `0x208e12` | `disproven` | Live item inspect/destroy repros on `2026-06-02` repeatedly logged `MulticlassItemUsability target=CanEquip ... caller_return_rva=0x208e12 inventory_scan_candidate ... returned_result=1` immediately before the client crashed; the same scan-family callers are hot during inventory churn, not equip intent | The raw item-class gate is too early and too shared on the inventory scan path. Widening usability there destabilizes inspect/destroy flows even though the broader multiclass item feature is valid elsewhere | Keep these inventory-scan callers observational and fail-closed; only override on proven equip/use paths |
| Item inspect / destroy stability | Keeping the `CInvSlot::HandleLButtonUp` helper family and its callsite probes armed during item interaction (`InvSlotHandleLButtonCore*`, `CInvSlotWnd::HandleLButtonUp`, related move-item traces) | `disproven` | After the inventory-scan `CanEquip` override was removed, the next live inspect crash on `2026-06-02` no longer showed `CanEquip ... returned_result=1`; instead the last hot lines were the `InvSlotHandleLButtonCore*` trace/callsite family plus `MoveItemSend` immediately before the client died | The shared invslot interaction stack is still too hot and too broad for safe inspect/destroy tracing in this client. Even observational hooks there are enough to destabilize item interaction | Keep item-interaction helper family disabled until a colder producer seam is proven; stabilize inspect/destroy first, then reintroduce narrower equip/use seams one by one |
| Inventory summary pane class text | Inventory-side trace arming through `CInventoryWindow::WndNotification` (`0x293490`) plus candidate A `0x290100` and candidate B `0x2905f0` | `disproven` | These hooks now install, but the user repro still produced no `InventorySummaryCandidateEntryTrace`, no `InventoryClassDisplayCorrelationWindow`, and no `InventoryClassDisplayTrace` from A/B | The installs are no longer the blocker; those arm points are not the live summary update path for the visible class line | Use binary-backed refresh/writer candidates instead of more generic callback guesses |
| Inventory summary pane class text | Binary-backed refresh candidate C at `0x312b00` (`VA 0x712b00`) | `disproven` | Live runs on `2026-05-22 08:11` and again on `2026-05-22 08:56` still showed `hook_manager: inventory summary candidate trace installed target=InventorySummaryRefreshCandidateC`, but no `InventorySummaryCandidateEntryTrace` during real inventory summary repros; the only correlated activity was unrelated shared-string noise after `WndNotification` | Static proximity to `GetClassDesc` and `GetRaceDesc` was not enough; this multi-label refresh function is not the live writer for the top-left inventory summary pane in the active repro | Preserve inventory correlation budget from unrelated shared-string lookups and pivot to `CInventoryWindow` self-refresh seams such as `Activate` / `OnProcessFrame` or other binary-backed summary producers |
| Inventory summary pane class text | `CInventoryWnd::OnProcessFrame` / candidate D at `0x292c90` | `inconclusive` | Live startup on `2026-05-22 09:55:23` logged `hook_manager: InventorySummaryRefreshCandidateD trace prologue unsupported; hook disabled`, so no `InventoryOnProcessFrameTrace` lines could fire in the repro | The seam is still promising and now concretely identified, but the current inline detour framework cannot patch this prologue as-is | Either extend the detour/trampoline support for this prologue shape or pivot to a nearby callable child seam inside `OnProcessFrame` such as `0x690100` / `0x691a90` / `0x6904b0` |
| Inventory summary pane class text | Imperative custom-label refresh via `CInventoryWindow::WndNotification` plus inventory-window global lookup and `SetWindowTextA` | `disproven` | Live runs on `2026-05-28 11:26` and `11:29` only logged `InventoryCustomClassLabelRefreshFailure reason="inventory_window_unavailable"`; in the later run there were no `InventoryWindowWndNotification` traces at all | A push-style custom label write depends on brittle window-pointer timing and an event seam that is not reliably hot for this surface | Use a pull-style custom `EQType` label backed by `__GetLabelFromEQ` instead of trying to locate and mutate the live label control |
| Inventory summary pane class text | Keeping the shared `CInventoryWindow::WndNotification` trace detour armed after the custom `EQType 6666` label path was already live | `disproven` | Live run on `2026-05-28 11:43` still installed `inventory window wnd notification trace`, and the close repro at `11:44` logged `MulticlassItemTrace target=InventoryWindowWndNotification ... notification_code=0xa` immediately before the user-reported inventory-close crash | The old notification detour was no longer needed for the visible class label and remained a risky close-time trace seam | Retire the notification detour from the inventory-label path and keep the working pull-style `__GetLabelFromEQ` hook only |
| Inventory summary pane class text | Treating `GetDeityDesc` as an already-usable discovery helper in its current hook form | `inconclusive` | Current code still models `GetDeityDesc` as `CDECL`, while `eqlib` treats it as a `CEverQuest` member; live installs previously failed with `prologue unsupported` / `install_failed` | Deity correlation is still useful, but the present hook shape should not be trusted as proof | Revisit `GetDeityDesc` only with the correct member-function model or caller-level tracing |

## Confirmed Useful Seams

These are not negative results; they are listed here so future work has a short record of what actually solved a visible surface.

| Surface | Confirmed seam | Evidence | What it means |
| --- | --- | --- | --- |
| `/who` row class text | Shared lookup target `0x7d0660` with visible class-label caller `0x477e6` remains the real hot seam | Earlier live proof on `2026-05-21` showed the seam can rewrite the visible `/who` class label, and the `2026-05-28` regression logs still show `caller_rva=0x477e6` as the active lookup site | The seam itself is still correct; the disproven piece was later feeding it the local player's class mask instead of the matched `/who` row subject | Keep `/who` work pinned to `0x477e6 -> 0x7d0660`, but source the formatted text from correlated `OP_WhoAllResponse` entries rather than local-only state |
| Guildmaster trainer access plus skill list | Active-guildmaster class-match callsite at `0x5374a1` / RVA `0x001374a1` plus a training-session-only local class override applied on successful `OP_GMTraining` and restored on `OP_GMEndTraining` | Live repro on `2026-05-22` first showed `MulticlassTrainerHook ... normalized_class_id=7 override_applied=true` and a successful `OP_GMTraining` send/recv; later packet decode proved the Monk GM payload already carried Monk-only caps, and the final session-scoped local class override made both Monk and Magician trainer windows render the correct class skill rows | The final clean-room fix is a two-step client path: clear the early guild access reject at the proved class-match seam, then temporarily align the local class context only for the active train window session | Do not reopen the old validator-entry hook or broad global class spoofing ideas unless a later client build breaks either the class-match callsite or the training-session restore path |

## Working Rules

- Before trying a new hook, check whether it is just a close cousin of an already `disproven` seam.
- When adding a new row, capture the exact RVA or function, the target surface, and one concrete runtime/disassembly reason it failed.
- Prefer upgrading `inconclusive` rows to `disproven` or `confirmed-useful`; avoid leaving ambiguous dead-end notes scattered across commits and handoff text.
